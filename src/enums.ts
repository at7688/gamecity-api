export enum WebPlatform {
  PC = 1,
  MOBILE = 2,
}

export enum LangType {
  EN = 'en',
  TW = 'tw',
  CN = 'cn',
}

export enum TargetType {
  PLAYER = 1,
  AGENT = 2,
}

export enum ValidateStatus {
  UNPROCESSED = 1,
  PROCESSING = 2,
  APPROVED = 3,
  REJECTED = 4,
}
